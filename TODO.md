# TODO

- array
- struct
  - field access
- pointers and references, including const
  - for passing structs in functions

## Built-in functions

- timer (0 <= id < 32)
  - void timer_start(id, timeout_ms)
  - void timer_reset(id)
  - bool timer_query(id)
  - void timer_stop(id)

- math
  - math_min & math_max
  - math_random
  - math_clamp
  - math_smoothstep
  - math_interpolate
  - math_lerp, math_slerp

 